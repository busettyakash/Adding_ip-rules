name: Add Database Firewall Rule

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment (dev/qa/prod)'
        required: true
        type: choice
        options:
          - dev
          - qa
          - prod
      developer_name:
        description: 'Developer name (e.g., john-doe)'
        required: true
        type: string
      ip_address:
        description: 'IP address to whitelist (e.g., 203.0.113.45 or 203.0.113.0/24)'
        required: true
        type: string

permissions:
  id-token: write
  contents: read

jobs:
  add-firewall-rule:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Set environment variables
        run: |
          case "${{ inputs.environment }}" in
            dev)
              echo "RESOURCE_GROUP=rvc-platform-nonprod-db-rg" >> $GITHUB_ENV
              echo "SERVER_NAME=rvcplatform-dev-db" >> $GITHUB_ENV
              ;;
            qa)
              echo "RESOURCE_GROUP=rvc-platform-nonprod-db-rg" >> $GITHUB_ENV
              echo "SERVER_NAME=rvcplatform-qa-db" >> $GITHUB_ENV
              ;;
            prod)
              echo "RESOURCE_GROUP=rvc-platform-prod-db-rg" >> $GITHUB_ENV
              echo "SERVER_NAME=rvcplatform-prod-db" >> $GITHUB_ENV
              ;;
            *)
              echo "Invalid environment"
              exit 1
              ;;
          esac

      - name: Add IP to database firewall
        run: |
          chmod +x cicd/updated-firewal1.sh
          ./cicd/updated-firewal1.sh \
            "${RESOURCE_GROUP}" \
            "${SERVER_NAME}" \
            "${{ inputs.ip_address }}"

      - name: Display current firewall rules
        run: |
          echo "✅ Successfully added IP ${{ inputs.ip_address }} for ${{ inputs.developer_name }}"
          echo ""
          echo "Current firewall rules for ${SERVER_NAME}:"
          az sql server firewall-rule list \
            --resource-group "${RESOURCE_GROUP}" \
            --server "${SERVER_NAME}" \
            --output table
